
export const metadata = {
  title: "V# | Sign in",
  description: "Sign in to your account.",
};

export default function RootLayout({ children }) {
  return (
    <>
      {children}
    </>
  );
}
