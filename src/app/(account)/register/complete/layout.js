
export const metadata = {
  title: "V# | Finish Registering",
  description: "Start shopping today.",
};

export default function RootLayout({ children }) {
  return (
    <>
      {children}
    </>
  );
}
