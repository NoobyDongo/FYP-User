
export const metadata = {
  title: "V# | Create New Account",
  description: "Start shopping today.",
};

export default function RootLayout({ children }) {
  return (
    <>
      {children}
    </>
  );
}
