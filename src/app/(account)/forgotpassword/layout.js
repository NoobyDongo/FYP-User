
export const metadata = {
  title: "V# | Forgot Password?",
  description: "Let's reset it.",
};

export default function RootLayout({ children }) {
  return (
    <>
      {children}
    </>
  );
}
